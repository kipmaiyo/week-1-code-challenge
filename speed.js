function speedlimitcheck(speed){
    let limitcheck = 70
    let points = 5
    let threshhold = 12
    if (speed <= limitcheck){
return “Ok”
    }
    else if  //(speed > limitcheck)
    (let exceedslimit = speed - limitcheck
     let points =Math.floor(speed/points) 
     if (points > threshhold)){
        return 'License suspended.'
  }
  else {
    return"points" + points
  }
} 
let speed = 80
statuscheck(speed)
console.log(statuscheck(speed))
     
     
     
     
     
     
     
